# server settings
PORT=3001
INVITE_API_SECRET=change_me_secret_for_prod

# SMTP (exemple — fournis tes infos SMTP here)
# .env (exemple Mailtrap)
SMTP_HOST=smtp.mailtrap.io
SMTP_PORT=2525
SMTP_USER=TON_USER_MAILTRAP
SMTP_PASS=TON_PASS_MAILTRAP
FROM_EMAIL="MonApp <no-reply@monapp.com>"


# frontend URL used to build accept link (optionnel)
FRONTEND_URL=http://localhost:5173

# frontend settings (put these in .env for Vite)
VITE_API_BASE=http://localhost:3001
VITE_INVITE_API_SECRET=change_me_secret_for_prod
VITE_FRONTEND_URL=http://localhost:5173